import * as React from 'react';
import { Link } from 'react-router-dom';
const Index = () => {
    return (
        <Link to="/Login">
            <button>Click</button>
        </Link>
    );
}
export default Index;